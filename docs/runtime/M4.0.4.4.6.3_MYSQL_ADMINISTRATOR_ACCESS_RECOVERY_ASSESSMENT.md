# M4.0.4.4.6.3 MySQL Administrator Access Recovery Assessment Report

## 1. Current Access Tests

Assessment scope:

```text
Read-only administrator access assessment only.
No password, account, privilege, database, or service changes.
```

Active endpoint:

```text
Executable: E:\MySQL Server 8.0.46\bin\mysqld.exe
Datadir: E:\MySQL Server 8.0.46\Data
TCP ports: 3306, 33060
```

Connection tests against the active instance:

```text
Configured labor_saving credential: FAIL (ERROR 1045)
Configured root credential: FAIL (ERROR 1045)
Root without password: FAIL (ERROR 1045)
Root administrator credential (operator-supplied historical line): PASS
```

The successful credential was supplied only for this manual verification and is not recorded here.

## 2. Existing Account Discovery

Read-only `mysql.user` discovery returned:

```text
mysql.infoschema  @localhost  account_locked=Y  password_expired=N
mysql.session     @localhost  account_locked=Y  password_expired=N
mysql.sys         @localhost  account_locked=Y  password_expired=N
root              @%          account_locked=N  password_expired=N
root              @127.0.0.1  account_locked=N  password_expired=N
root              @::1        account_locked=N  password_expired=N
root              @localhost  account_locked=N  password_expired=N
```

The configured `labor_saving` account was not returned. No additional application administrator account was observed.

## 3. Authentication Plugin Review

Observed plugins:

```text
root@%          mysql_native_password
root@127.0.0.1  mysql_native_password
root@::1        mysql_native_password
root@localhost  caching_sha2_password
mysql.*         caching_sha2_password
```

Server authentication variables:

```text
skip_name_resolve=1
require_secure_transport=0
```

The active root accounts are not locked and are not password-expired. Authentication plugin choice differs for `root@localhost` versus the loopback TCP accounts and must be retained during any future, explicitly approved recovery.

## 4. Host Matching Review

Successful administrator host resolution:

```text
--host=127.0.0.1  -> CURRENT_USER() = root@127.0.0.1
--host=localhost   -> CURRENT_USER() = root@::1
--host=::1         -> CURRENT_USER() = root@::1
```

Because `skip_name_resolve=1`, host matching is address-oriented. The `root@127.0.0.1` and `root@::1` rows are the effective accounts for the tested TCP paths; `root@localhost` is not the account selected by the IPv6 TCP test.

## 5. Credential Recovery Options

Available now:

- Use the verified administrator credential for read-only diagnostics under an approved operator session.
- Preserve the active instance and avoid any account or privilege mutation during this assessment.

Requires a separate controlled approval:

- Restore or create the approved `labor_saving` migration account with the required authentication and host scope.
- Verify the account against the approved target database and migration permission set.
- Generate and verify a real pre-migration backup before any migration execution review.

No recovery option was executed in this stage. `skip-grant-tables`, `init-file`, service restart, and process termination remain prohibited.

## 6. Security Risk Assessment

Controls preserved:

- No `ALTER USER`, `CREATE USER`, `GRANT`, or `REVOKE` was run.
- No password was changed or written to a document, script, or Git history.
- No `skip-grant-tables` or `init-file` mode was used.
- No service restart or `mysqld` termination was performed.
- No migration, seed, reset, or database creation was performed.

Risks:

- Administrator access is available, but the configured migration account is absent or unusable.
- Multiple root Host rows and mixed authentication plugins increase the risk of validating the wrong effective account.
- Administrator access must not be treated as approval to bypass the configured migration-user and backup gates.

Risk decision:

```text
Security Risk Assessment: CONTROLLED / MIGRATION ACCESS NOT READY
```

## 7. Recommended Next Action

1. Keep the verified administrator credential out of repository files and environment commits.
2. In a separately approved access-recovery change, restore the `labor_saving` account with an explicit host and plugin decision.
3. Re-run the configured migration-user connection test for both `localhost` and `127.0.0.1`.
4. Reconfirm the approved target database, backup/restore procedure, and migration permission set before any execution decision.

Administrator access decision:

```text
ACCESS_AVAILABLE
```

This result confirms administrator authentication only. It does not authorize permission changes or migration execution, and the configured migration-user path remains unavailable.
