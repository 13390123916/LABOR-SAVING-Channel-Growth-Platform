# M4.0.4.4.4 MySQL Instance Identity & Authentication Recovery Report

## 1. Instance Identity

Runtime identity evidence:

```text
Windows service: MySQL
Service state: RUNNING
Service binary: E:\MySQL Server 8.0.46\bin\mysqld.exe
Service config: E:\MySQL Server 8.0.46\my.ini
Data directory: E:\MySQL Server 8.0.46\Data\
Server UUID: b2f87b55-87dc-11f1-af51-84ba5929e406
Version: 8.0.46
Hostname: LAPTOP-CEGJVFIQ
Port: 3306
```

Instance identity summary:

- The live MySQL runtime is the local `E:\MySQL Server 8.0.46` installation.
- The actual server UUID matches the local `Data` directory `auto.cnf`.
- The current target database `labor_saving_channel_growth` is not present in this instance.

Decision:

```text
Instance Identity: PARTIAL
```

## 2. Port Ownership

Port evidence:

```text
TCP 0.0.0.0:3306 LISTENING PID 8124
Service PID 6072
```

Additional process evidence:

- `mysqld` PID `6072` is responding.
- `mysqld` PID `8124` is responding.

Decision:

```text
Port Ownership: PARTIAL
```

The service PID and port owner PID differ, so the runtime ownership should be treated as ambiguous until an operator confirms which `mysqld.exe` owns the exposed MySQL endpoint.

## 3. Connection Validation

Loaded `DATABASE_URL` source:

```text
website/.env
```

Parsed target:

```text
host=localhost
port=3306
username=labor_saving
database=labor_saving_channel_growth
```

Configured migration user validation:

```text
mysql --host=localhost --user=labor_saving ... SELECT 1
ERROR 1045 (28000): Access denied for user 'labor_saving'@'::1'

mysql --host=127.0.0.1 --user=labor_saving ... SELECT 1
ERROR 1045 (28000): Access denied for user 'labor_saving'@'127.0.0.1'
```

Instance connection validation with local bootstrap root credential:

```text
mysql --host=localhost --user=root ... SELECT CURRENT_USER(), @@server_uuid, @@datadir, @@port, @@hostname
root@::1 b2f87b55-87dc-11f1-af51-84ba5929e406 E:\\MySQL Server 8.0.46\\Data\\ 3306 LAPTOP-CEGJVFIQ

mysql --host=127.0.0.1 --user=root ... SELECT CURRENT_USER(), @@server_uuid, @@datadir, @@port, @@hostname
root@127.0.0.1 b2f87b55-87dc-11f1-af51-84ba5929e406 E:\\MySQL Server 8.0.46\\Data\\ 3306 LAPTOP-CEGJVFIQ
```

Decision:

```text
Connection Validation: BLOCKED
```

The instance is reachable, but the configured migration user cannot authenticate.

## 4. User Authentication Status

`mysql.user` evidence:

```text
root@localhost        caching_sha2_password
root@127.0.0.1        mysql_native_password
root@::1               mysql_native_password
root@%                 mysql_native_password
```

No `labor_saving` row was returned in the `mysql.user` review query.

Permission review for the authenticated root account:

```text
SHOW GRANTS FOR CURRENT_USER()
GRANT ... CREATE, DROP, INDEX, ALTER ... ON *.* TO `root`@`::1` WITH GRANT OPTION
```

Decision:

```text
User Authentication Status: BLOCKED
```

The configured migration user is not authenticated, so the required migration user path is not recovered.

## 5. Database Status

Existing databases:

```text
information_schema
laborsaving
laborsaving_auth
mysql
performance_schema
sys
```

Target database check:

```text
labor_saving_channel_growth: NOT FOUND
```

Tooling validation:

```text
E:\MySQL Server 8.0.46\bin\mysql.exe: PRESENT
E:\MySQL Server 8.0.46\bin\mysqldump.exe: PRESENT
mysql.exe PATH lookup: missing
mysqldump.exe PATH lookup: missing
docker PATH lookup: missing
```

Decision:

```text
Database Status: BLOCKED
```

The target database required by the approved migration path does not exist in the active instance.

## 6. Risk Analysis

Risks:

- The live MySQL service is real, but the exposed port and service PID do not match cleanly.
- The configured `labor_saving` user cannot authenticate on either `localhost` or `127.0.0.1`.
- The target database `labor_saving_channel_growth` is absent from the active instance.
- Backup preparation cannot be completed for a missing target database.
- Treating root access as a substitute for the approved migration user would bypass the recovery boundary.

Controls applied:

- No `ALTER USER`, `CREATE USER`, or `GRANT` was run.
- No migration execution was run.
- No schema or migration SQL was modified.
- No frozen architecture was modified.
- No seed or reset was run.
- No backup artifact was fabricated.

Risk decision:

```text
Risk Analysis: BLOCKED
```

## 7. Recovery Decision

M4.0.4.4.4 MySQL Instance Identity & Authentication Recovery:

```text
BLOCKED
```

Blocking conditions:

- The approved target database `labor_saving_channel_growth` is not present in the active instance.
- The configured `labor_saving` user cannot authenticate.
- Port ownership remains ambiguous between the service PID and the listening PID.

Required unblock evidence:

- Confirm the correct MySQL runtime instance and port owner.
- Restore a valid authenticated migration user for the approved target database.
- Confirm the target database exists on the active instance.
- Re-run `db:health`, `db:migrate:status`, and `db:tx-smoke` successfully.
- Prepare a real pre-migration backup artifact before any execution review.
