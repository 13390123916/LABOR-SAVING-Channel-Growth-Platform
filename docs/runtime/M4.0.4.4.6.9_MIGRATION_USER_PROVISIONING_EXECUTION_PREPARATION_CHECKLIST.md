# M4.0.4.4.6.9 Migration User Provisioning Execution Preparation Checklist Report

## Scope

This is the final human-preparation checklist before any database write
operation. It records what is still missing after the governance review PASS
and authorization BLOCKED result from M4.0.4.4.6.8. It does not execute or
authorize `CREATE USER`, `ALTER USER`, `GRANT`, `REVOKE`, database creation or
deletion, migration deploy, seed, reset, or service changes.

## 1. Approved Operator Evidence

Required before execution:

- named operator and role;
- approving authority and change ticket;
- approved maintenance window;
- evidence repository/location;
- explicit stop and escalation contact.

```text
Preparation status: BLOCKED
Evidence: NOT PROVIDED
```

## 2. Execution Authority Evidence

The approval must explicitly cover the exact username, host rows,
authentication plugin, target schema, privilege manifest, rollback authority,
validation sequence, and stop conditions. Administrator access alone is not
execution authority.

```text
Preparation status: BLOCKED
Evidence: NOT PROVIDED
```

## 3. Target Database Provisioning Evidence

The operator must independently confirm the active instance and the existence,
owner, environment, and identity of `labor_saving_channel_growth` on:

```text
E:\\MySQL Server 8.0.46
```

No database creation or drop is part of this checklist.

```text
Preparation status: BLOCKED
Target database: NOT CONFIRMED
```

## 4. DATABASE_URL Confirmation

Provide an explicit process-level `DATABASE_URL` outside repository files and
verify that host, port, and database match the approved target. `.env`,
`.env.example`, fallback values, and local development defaults do not qualify.

```text
Preparation status: BLOCKED
Explicit approved DATABASE_URL: NOT PROVIDED
```

## 5. Secret Injection Readiness

The `labor_saving` secret must be available from an approved external secret
manager, with owner, rotation, revocation, exposure response, and cleanup
records. Injection must avoid Git, reports, shell history, logs, and CI-visible
values.

```text
Preparation status: BLOCKED
External secret source/injection evidence: NOT PROVIDED
```

## 6. User Provisioning Parameters

The approval package must freeze the following values before execution:

```text
Username: labor_saving
Purpose: migration identity only; separate application identity required
Target database: labor_saving_channel_growth
```

Any rename, shared application use, or target change requires a new review.

```text
Preparation status: BLOCKED
Identity approval and account-state evidence: NOT PROVIDED
```

## 7. Host Scope Confirmation

With `skip_name_resolve=1`, approve explicit IP rows only:

```text
Preferred: labor_saving@127.0.0.1
Optional:  labor_saving@::1 only if dual-stack is approved
Forbidden: wildcard, hostname, or broad network rows
```

The canonical `DATABASE_URL` host must match the approved row set.

```text
Preparation status: BLOCKED
Host scope confirmation: NOT PROVIDED
```

## 8. Authentication Plugin Confirmation

Confirm `caching_sha2_password` compatibility with the installed connector and
secure connection path, plus account lock/expiry policy and secret rotation.
Any use of `mysql_native_password` requires separate compatibility and security
approval; it is not an automatic fallback.

```text
Preparation status: BLOCKED
Plugin confirmation: NOT PROVIDED
```

## 9. Privilege Confirmation

Approve an exact target-schema-only manifest covering only the capabilities
needed to apply and inspect the approved Prisma migration history, tables,
indexes, and constraints. Explicitly exclude global administration, account
management, `GRANT OPTION`, replication, shutdown, file access, and unrelated
databases. Keep application privileges separate from migration DDL.

```text
Preparation status: BLOCKED
Privilege manifest/approval: NOT PROVIDED
```

## 10. Backup Artifact Preparation

Before any account or privilege mutation, prepare and verify a backup for the
confirmed target database. Record target identity, timestamp, operator,
retention location, integrity verification, and restore owner.

```text
Preparation status: BLOCKED
Verified backup artifact: NOT AVAILABLE
```

The running MySQL service is not a backup and administrator access does not
replace backup evidence.

## 11. Rollback Preparation

Prepare a read-only pre-change snapshot of account rows, authentication
metadata, grants, target identity, and approval metadata. Obtain explicit
rollback authority for stopping before creation, removing only a newly created
account, restoring altered account state, and handling partial failure.

Later migration rollback must use the database backup and its own runbook.
Record failure point, operator, timestamp, action, result, and residual risk.

```text
Preparation status: BLOCKED
Rollback authority/evidence: NOT PROVIDED
```

## 12. Validation Commands

The operator must have an approved, read-only validation sequence prepared to:

1. confirm account existence only on approved host rows;
2. confirm plugin, lock/expiry state, and connection identity;
3. confirm target database identity and least-privilege login;
4. compare exact grants with the approved manifest;
5. verify absence of global, unrelated-database, account-management, and
   `GRANT OPTION` privileges;
6. record output without credentials;
7. stop before Prisma migration execution, which has a separate gate.

```text
Preparation status: PASS as documented procedure
Live validation: NOT EXECUTED
```

## Final Preparation Decision

| Preparation item | Status |
| --- | --- |
| Approved Operator evidence | BLOCKED |
| Execution authority evidence | BLOCKED |
| Target database evidence | BLOCKED |
| `DATABASE_URL` confirmation | BLOCKED |
| Secret injection readiness | BLOCKED |
| User provisioning parameters | BLOCKED |
| Host scope confirmation | BLOCKED |
| Authentication plugin confirmation | BLOCKED |
| Privilege confirmation | BLOCKED |
| Backup artifact preparation | BLOCKED |
| Rollback preparation | BLOCKED |
| Validation procedure | PASS as documented; not executed |

Final status:

```text
M4.0.4.4.6.9 Migration User Provisioning Execution Preparation Checklist: BLOCKED
READY FOR HUMAN EXECUTION: NO
Database write operations: NOT AUTHORIZED
```

The checklist is complete as a governance artifact, but human execution is
not ready until every blocking preparation condition is evidenced and approved.
No database write operation was executed.
